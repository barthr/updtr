defmodule UpdtrWeb.PasswordResetController do
  use UpdtrWeb, :controller

  def request_reset_password(conn, _params) do
    conn
  end

  def handle_reset_password(conn, _params) do
    conn
  end
end
